#! /bin/bash
export http_proxy="http://proxy.nagaokaut.ac.jp:8080"

SSID=$(iwlist wlan0 scan |grep SSID)
if [[ $SSID =~ '.*Yamamoto.*' ]]
then
    echo $SSID
    export HTTP_PROXY=http://proxy.nagaokaut.ac.jp:8080
fi
#setenv HTTP_PROXY http://proxy.nagaokaut.ac.jp:8080
